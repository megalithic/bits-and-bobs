/* @flow */
import React from 'react'
import { mount } from 'enzyme'
import {{+~expand('%:p:h:t')+}} from '.'

test('{{++}}', () => {
  const output = mount(<{{++}} />)
  const {{++}}Node = output.find('#{{+~\~1+}}')
  expect({{+~\~2+}}Node.{{++}}).toBe({{++}})
})
