name: "welltok"
windows:
  - name: "chats"
    root: "~"
    panes:
      - irc
  - name: "services"
    root: "~/code/welltok"
    layout: tiled
    focus: true
    panes:
      - commands:
        - cd currency-service
        - git co master
        - npm install
        - npm run dev
      - commands:
        - cd achievement-service
        - git co master
        - npm install
        - npm run dev
      - commands:
        - cd munero
        - git co master
        - npm install
        - npm run dev
      - commands:
        - cd ep-dev-edition
        - docker-machine start dev
        - eval $(docker-machine env dev)
        - make reset
        - cd ~/code/welltok
        - docker ps
        - cd rewards-dashboard
        - npm run mock
        focus: true
      - commands:
        - cd cafewell
        - chruby ruby-2.2.3
        - npm install
        - bundle
        - bundle exec rake db:migrate
        - die redis
        - killport 5000 # kill moxy in case it's already running
        - killport 3000 # kill thin/rails in case it's already running
        - be foreman start
      - commands:
        - cd rewards-dashboard
        - npm install
        - npm start
      # - commands:
      #   - cd rewards-wallet-widget
      #   - npm install
      #   - npm start
  - name: "code"
    root: "~/code/welltok/rewards-dashboard"
    panes:
      - ls -gha
